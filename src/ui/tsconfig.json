{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    /* only runtime types here */
    "types": ["node", "next"],
    "allowJs": false,
    "isolatedModules": true
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "app",
    "images.d.ts"
  ]
}
